define( function( ) {
    return function(sandbox){
        
        sandbox.models.Count = BaseBone.Model.extend({
            defaults:{
                count:0
            }
        });

         

    };
});
